# design-fidelity/SKILL.md
> 원본 위치: ~/.claude/skills/design-fidelity/SKILL.md (대화기록에서 추출한 사본)


같은 구현물을 여러 번 재검토하게 만드는 원인은 함정 목록의 부재가 아니라
작업 규칙의 부재다. 아래 규칙은 프로젝트를 가리지 않고 적용한다.

## Part 1 — Figma ↔ 코드 픽셀 일치

**철칙: 구현은 기억이 아니라 Figma 노드를 매번 다시 읽고 옮긴다.**
사용자가 Figma를 손으로 수정하는 워크플로우에서는 내 기억이 항상 낡아 있다.
토큰 문서(design.md 등)만 보고 구현하는 것도 금지 — 문서는 노드의 최신 상태가 아니다.

### 구현 절차 (컴포넌트/화면마다)

1. **링크를 받으면 무조건 재독.** 사용자가 Figma 링크를 줄 때마다 — 이미 본
   노드여도, 방금 본 노드여도 — `get_design_context`(px·토큰·모든 속성) +
   `get_metadata`(좌표)를 다시 뽑고 시작한다. 링크가 여러 개면 전부.

2. **속성 diff 표 (눈대중 대체 — 제일 중요).** 노드마다 모든 속성을 표로 뽑아
   코드 값과 한 줄씩 대조한다:
   - font weight · size · **letterSpacing**(%→px 변환 확인) · lineHeight · color
   - **padding** · gap · radius · **justify/align**(justify-between을 고정 gap으로 만들지 말 것)
   - ✗가 0이 될 때까지. **"비슷하면 OK" 절대 금지.**
   - 사용자가 어긋난 곳을 하나씩 집어주는 상황 = 이 표를 안 만들었다는 신호.

3. **실제 애셋 사용.** 도형(화살표·말풍선 꼬리·아이콘·곡선)은 CSS/근사로 그리지 않는다.
   asset URL을 받아 SVG path **그대로** 사용. 방향 전환은 `scaleX/scaleY(-1)`.

4. **선제 전수 대조.** 신고를 기다리지 않는다. 어떤 축(아이콘·타이포·컬러·spacing)을
   구현하거나 수정했으면, 그 축을 디자인 파일 전체에서 전수 스캔해 코드와 1:1
   대조표를 만들고 어긋난 것을 전부 고친 뒤 결과를 보고한다.
   신고가 들어온 경우에도 그 지점만 고치지 말고 같은 축 전체를 훑는다 —
   하나가 어긋났으면 형제들도 어긋나 있을 확률이 높다.

5. **렌더 대조 후에만 "완료".** dev 서버 → headless 스크린샷 → 크롭 → 확대 →
   Figma 렌더와 비교. 데이터가 달라 전체 픽셀 diff는 노이즈가 크므로
   속성 표가 주(主), 렌더는 형태 확인 보조.

6. **받은 노드는 전부 확인.** 링크 여러 개·오버뷰 프레임이면 화면이 여러 장이다.

### 함정 기록 규칙

- API가 **에러 없이 조용히 무시하는 값**(예: strokeWeight 변수 바인딩, 아이콘 width
  바인딩, alias+알파 동시 지정, clipsContent 기본 true, opacity 단위 0–1 vs 0–100)을
  발견하면 즉시 프로젝트의 디자인 문서 Known Gaps에 우회법과 함께 한 줄 기록.
- 단, 기록은 같은 함정의 재발만 막는다. 충실도 재작업을 막는 것은 위의 1–6.

## Part 2 — 한국어 UX 라이팅

1. **조사는 유틸로.** `{이름}가` 식 하드코딩 금지. 받침 유무로 이/가·은/는·을/를·
   과/와를 고르는 유틸 하나를 만들어 문장 조립 지점에 적용한다.
   한글 음절이 아닌 끝(영문·숫자)은 받침 없음으로 폴백.

2. **용어 변경은 전수 치환.** 사용자가 용어를 확정하면(A→B) 코드·디자인 문서
   전역을 치환하고 **잔여 검색 0을 확인**해 보고한다. 과거 기록 성격 문서
   (포트폴리오·회고·번들)는 남기되, 남겼다는 사실을 명시적으로 보고.

3. **동적 문구는 상태별 문장으로 중앙화.** `{변수}보다 {상태}` 식 템플릿 조립은
   조합에 따라 비문을 만든다. 상태별로 자연스러운 완성 문장을 통째로 정의하고
   한 곳에서 관리하며, 변수가 바뀌면 조사·어미도 함께 맞는지 모든 조합을 확인한다.

4. **중복·기준 혼재를 스스로 점검.** 한 화면에 비교 기준·지표가 3개 이상 섞이면
   사용자가 지적하기 전에 먼저 정리를 제안한다. 같은 정보가 타이틀과 캡션에
   중복되면 한쪽을 뺀다.

5. **로딩·빈 상태 문구도 일관되게.** 같은 성격의 상태 문구("확인 중/파악 중" 등)와
   타이포가 화면마다 다르면 하나로 맞춘다. 데이터가 없으면 숨기지 말고
   "없다"고 보여준다.

## 신고를 받았을 때의 행동 순서

사용자(디자이너)는 실행된 화면에서 이상(문구·데이터·정렬)을 발견해 신고하는
역할이고, 원인 규명과 수리는 전적으로 내 몫이다. 신고가 들어오면:

1. 증상 지점을 바로 고치지 않는다. 먼저 그 값·문구·스타일이 **어디서 조립되는지**
   근원을 추적한다.
2. 근원 한 곳을 고친다 (증상 지점마다 개별 패치 금지).
3. 같은 근원을 쓰는 **다른 자리 전부**를 찾아 같이 확인한다 — 하나가 어긋났으면
   형제들도 어긋나 있을 확률이 높다.
4. "원인은 X였고, 같은 패턴인 Y·Z도 확인/수정했다"까지 보고한다.

## 완료 선언 전 체크리스트

아래 전부 통과하기 전에는 "완료"·"반영됐다"고 말하지 않는다:

- [ ] 사용자가 준 Figma 링크/노드를 **전부** 이번 턴에 재독했다 (기억 사용 안 함)
- [ ] 속성 diff 표를 만들었고 ✗가 0이다
- [ ] 이번에 건드린 축(아이콘·타이포·컬러·spacing)을 전수 대조했다
- [ ] 렌더 스크린샷을 Figma와 실제로 비교했다
- [ ] (용어/문구 변경 시) 전역 치환 후 잔여 검색 결과가 0이다
- [ ] (동적 문구 변경 시) 변수·상태 모든 조합에서 조사·어미가 자연스럽다
