# H1 검증 테스트 키트 — "신호가 판단을 빠르게 하는가"

> 목적: 포트폴리오 P4(사용자 검증) 페이지의 [기입] 자리를 실제 수치로 채우기.
> 소요: 참가자 1명당 약 15분 × 5명 = 반나절이면 충분.

---

## 1. 설계 요약

| 항목 | 내용 |
|---|---|
| 가설 (H1) | 숫자·그래프보다 신호가 먼저 와야 "지금 사도 될까"를 빠르고 확신 있게 판단한다 |
| 참가자 | 5명 — 장보기를 직접 하는 지인 우선 (1인가구면 더 좋음) |
| 비교안 | A안: 현재 앱(신호 우선) / B안: 신호 칩을 가리고 가격·차트만 보이게 한 화면 |
| 과제 | 품목 3개(예: 오이·감자·사과)씩 "지금 사도 될까요? 사도 됨/애매함/사면 손해 중 답해주세요" |
| 순서 편향 방지 | 참가자 절반은 A→B, 절반은 B→A 순서로. 같은 품목을 두 안에서 반복하지 않기 |

## 2. 측정 항목 (품목마다 기록)

1. **판단 소요 시간** — 화면 제시 → 답변까지 초 단위 (스마트폰 스톱워치면 충분)
2. **판단 일치율** — 참가자 답변이 데이터 기준 정답(신호등 레벨)과 일치하는가 (O/X)
3. **확신도** — 답변 직후 "그 판단, 얼마나 확신하세요? 1~5점"

## 3. 진행 스크립트 (그대로 읽기)

> "마트에서 장을 보다가 이 화면을 봤다고 상상해주세요. 지금 이 품목을 사도 될지, '사도 됨 / 애매함 / 사면 손해' 중 하나로 답해주시면 됩니다. 정답을 맞히는 시험이 아니라 화면이 판단에 도움이 되는지 보는 테스트예요. 소리 내어 생각하면서 봐주셔도 좋습니다."

과제 후 공통 질문 (정성):
- "어느 쪽 화면이 더 판단하기 쉬웠나요? 왜요?"
- "헷갈리거나 못 믿겠는 표현이 있었나요?" ← **P4 '반영' 칸의 재료**

## 4. 기록 시트

| 참가자 | 순서 | 품목 | 안 | 시간(초) | 일치(O/X) | 확신도(1-5) |
|---|---|---|---|---|---|---|
| P1 | A→B | 오이 | A | | | |
| P1 | A→B | 감자 | A | | | |
| ... | | | | | | |

## 5. 집계 → 포트폴리오 기입

- 판단 시간: A안 평균 vs B안 평균 → P4 큰 숫자 자리
- 확신도: A안 평균 vs B안 평균 → 대본 "확신도도 N점 높았습니다"
- 정성 발견 1~2개 + 반영한 디자인 변경 → P4 '반영' 카드

## 6. 주의

- 수치를 만들어 넣지 말 것 — 면접 꼬리 질문("어떻게 측정했어요?")에 즉답할 수 있어야 함
- 결과가 가설과 반대로 나와도 그 자체가 좋은 페이지가 됨 ("가설이 깨져서 이렇게 수정했다"는 오히려 강한 서사)
- 테스트 전까지는 P4를 발표에서 빼는 게 안전

---

## 7. 재삽입용 — 사용자 검증 페이지 대본 (테스트 완료 후)

덱 재삽입: `validation-page-snippet.html`의 블록을 "팀 기여" 페이지 앞에 붙여넣고 페이지 번호를 갱신하면 됨 (Claude에게 요청해도 됨).

### 대본 (팀 기여 앞 페이지)

> ⚠️ 테스트 완료 후 [대괄호] 자리에 실제 수치를 넣을 것. 돌리기 전에는 이 페이지를 발표에서 빼는 게 안전.

프로세스만 검증한 게 아니라, 만든 것이 실제로 작동하는지도 확인했습니다. 처음 세웠던 가설 H1 — 신호가 숫자보다 먼저 와야 빠르게 판단한다 — 로 돌아가서, 5명에게 태스크 테스트를 돌렸습니다. 같은 품목을 신호 우선 화면과 숫자·그래프 우선 화면으로 각각 보여주고, "지금 사도 될까?"를 판단하게 한 뒤 소요 시간과 판단 일치율, 확신도를 쟀습니다.

결과는 [결과 요약 — 예: 신호 우선 화면에서 판단이 평균 N초 빨랐고, 확신도도 N점 높았습니다]. 그리고 테스트에서 [발견한 문제]가 드러나 [반영한 변경]으로 이어졌습니다.

5명짜리 작은 테스트지만, 이후 모든 디자인 논쟁에서 "내 감"이 아니라 이 데이터를 근거로 말할 수 있게 됐습니다.

---

