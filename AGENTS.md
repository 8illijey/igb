# 이거비싸 — 작업 규칙

## 1. 라이브 동작은 실행해서 확인한다. 주석을 믿지 않는다

**코드를 바꾸기 전에 돌려라:**

```bash
cd mobile && npm run check-live
```

지금 평년이 KAMIS 공식값인지 우리 근사치인지, verdicts가 언제 갱신됐는지, 우리 값이 공식값과 몇 % 벌어져 있는지를 한 번에 보여준다. 30초면 끝난다.

### 왜 이 규칙이 있나 (2026-09-17 사고)

`build-baselines.mjs` 헤더에 2026-08-18자 기록이 있었다 — "KAMIS가 데이터센터 IP를 차단해서 미러를 쓴다 → 미러엔 평년(dpr7)이 없다 → 워커가 우리 값을 주입한다 → 앱이 '평년'이라 부르는 값은 실은 최근 1년 평균이다."

이걸 현재 상태로 읽고, **멀쩡히 돌아오던 KAMIS 공식 평년을 우리 근사치로 갈아끼우는 변경을 여러 라운드에 걸쳐 제안**했다. 실제로는 그사이 KAMIS가 복구됐고 워커는 원래부터 KAMIS를 1순위로 부른다.

무엇이 잘못됐나:

- **과거 진단을 현재 상태로 읽었다.** 날짜가 적혀 있었는데도.
- **어긋나는 증거를 가설의 확인으로 읽었다.** 라이브 값이 우리 값과 다른 걸 보고 "역시 고장났다"로 단정했다. 같은 관찰이 "공식값과 근사치가 예상만큼 다르다"로도 읽힌다는 걸 검토하지 않았다.
- **틀린 전제 위에 계산을 쌓았다.** 계산 자체는 전부 맞아서 전제가 더 단단해 보였다.

**한 번의 `curl`이면 막을 수 있었다.** 워커 응답에 `day7: "일평년"` 라벨과 `dpr5`·`dpr6` 실값이 있으면 KAMIS 원본이고, 미러 변환 경로는 그 둘을 `'-'`로 비운다.

### 적용 범위

외부에 의존하는 모든 것 — KAMIS, 공공데이터포털 미러, 쿠팡 파트너스, Cloudflare Workers, Vercel, GSC. 이들의 가용성·응답 형식·한도는 **주석이 쓰인 뒤 바뀐다.**

라이브 동작을 단정하는 문장을 쓰기 전에, 근거가 실측인지 주석인지 스스로 물어라. 주석이면 실측으로 바꿔라.

### 같은 실수를 세 번 했다 — 세 가지 형태를 다 적어 둔다

2026-09-17 하루에 **똑같은 오류를 세 번** 냈다. 전부 "지금 눈앞의 것"과 "그때의 것"을 구분하지 않아 생겼다. 형태가 달라 매번 새로운 실수처럼 보였다는 게 핵심이다.

| # | 무엇을 봤나 | 무엇으로 착각했나 | 막는 법 |
|---|---|---|---|
| 1 | `build-baselines.mjs` 헤더의 8/18 진단 | 현재 시스템 상태 | `npm run check-live` |
| 2 | 에이전트가 방금 고친 `worker/README.md` | 그 파일의 원래 내용 | `git show HEAD:<파일>` |
| 3 | 캐시된 `origin/main` 참조 | GitHub의 현재 상태 | `git fetch` 먼저 |

**규칙 — 시점을 말할 수 없으면 단정하지 마라.**

- "이 파일은 원래 X라고 적혀 있었다"고 말하려면 **작업 트리를 읽지 말고** `git show HEAD:<파일>`로 확인하라. 방금 누군가(나 자신 포함)가 고쳤을 수 있다. 특히 서브에이전트에게 편집을 시킨 뒤에는 반드시.
- 원격 저장소 상태를 말하려면 **`git fetch`를 먼저** 하라. `origin/main`은 마지막 fetch 시점의 사본이다.
- 외부 시스템 동작을 말하려면 **호출해 보라.** 주석·문서·기억은 근거가 아니다.
- 서브에이전트 보고와 내 관찰이 엇갈리면, **내 관찰 쪽을 먼저 의심하라.** 에이전트는 편집 직전의 파일을 봤고 나는 편집 직후의 파일을 본다.

문장을 쓰기 전에 한 번만 물어라 — **"이건 언제 기준의 사실인가?"** 답할 수 없으면 확인하고 나서 써라.

## 2. 상태를 적을 때는 날짜와 확인 방법을 같이 적는다

이 저장소의 주석은 상세하고 근거가 많다. 그래서 더 믿음직하게 느껴지고, 그게 함정이다.

- **설계 의도·트레이드오프**는 안 낡는다. 지금처럼 자세히 써라.
- **외부 시스템의 상태**는 낡는다. 이렇게 구분해서 써라:

```js
// [2026-08-18 기록 — 당시 상태] 미러가 축산 도매 시계열을 안 줬다.
// 지금도 그런지: npm run check-live
```

고쳐진 과거 진단은 **고쳐졌다는 사실까지** 적어라. "X가 문제였다"만 남기면 다음 사람이 X가 아직 문제라고 읽는다.

## 3. 데이터 출처 요약 (2026-09-17 실측)

| 화면의 값 | 출처 | 비고 |
|---|---|---|
| 오늘 가격 | KAMIS `dpr1~4` (워커 경유) | 원본 1순위, 실패 시 미러 |
| **이맘때 평균** | **KAMIS 공식 `dpr7`(일평년)** | 우리가 계산한 값이 아니다 |
| 월별 계절 패턴 | `baselines.json` | KAMIS가 월별 평년을 안 줘서 우리가 계산. 공식값의 근사치(오차 중앙값 3.2%) |
| 판정(싸/적정/비싸) | `verdicts.json` | CI가 하루 1회 사전계산 |
| 쿠팡 상품 | `coupang-products.json` | 파트너스 API, 일 1회 |

`baselines.json`과 KAMIS `dpr7`은 **같은 개념의 다른 숫자**다. 한 화면에서 둘을 금액으로 같이 쓰면 사용자가 모순을 본다.

### `baselines.json`은 왜 있고 왜 안 쓰이나 (2026-09-17 결정: 보험으로만 유지)

**지금 라이브 화면에 닿는 곳이 0곳이다.** 그런데도 주 1회 CI를 돌리고 337KB를 유지한다. 지우고 싶어질 때 아래를 먼저 읽어라.

- **만든 이유 (2026-08-18)** — KAMIS가 데이터센터 IP를 막아 `dpr7`을 아예 못 받던 시기가 있었다. 그때 워커가 임시로 verdicts 값을 넣었는데 verdicts도 워커를 거쳐 만들어져 **순환**이었다(56종 중 50종에서 `normal === months[이번달]`). 앱이 '평년'이라 부르던 값이 실은 자체 1년 평균이었다. 그 순환을 끊으려고 원천 가격만으로 다시 계산한 것이다.
- **안 쓰는 이유 (2026-09-17)** — KAMIS가 복구됐다. 워커가 KAMIS를 1순위로 부르므로 공식 `dpr7`이 그대로 온다. 만든 이유였던 "받을 수 없음"이 사라졌고, 우리 계산은 공식값 대비 오차 중앙값 3.2%라 대체할 이유가 없다.
- **그래도 남기는 이유** — ① **보험**: KAMIS가 또 막히면 워커가 자동으로 이걸 쓴다(`baselineOf`). 2026-07~08에 실제로 겪었고, 없으면 그때 앱 홈이 통째로 빈다. ② **월별 계절 패턴(`normalMonths`)의 유일한 출처**: `dpr7`은 요청 날짜 하나의 값 한 개라, 12개월 곡선을 KAMIS로 만들려면 90품목 × 12~365회를 물어야 한다.

자세한 경위는 `mobile/scripts/build-baselines.mjs` 헤더와 `.github/workflows/baselines.yml` 주석에 같은 내용으로 적어 두었다.

## 4. 쿠팡 파트너스 API — 한도를 절대 올리지 마라

분당 한도 초과가 누적되면 계정이 제한된다. 2026-08-17과 2026-09-09에 실제로 두 번 당했다. `MAX_SAFE_RPM`은 올리지 말고, `--dry-run --only=`로 좁혀서 디버깅하라. 자세한 건 `mobile/scripts/README-coupang.md`.

## 5. 파일별 안내

- `mobile/AGENTS.md` — Expo 버전 주의
- `mobile/scripts/VERDICTS.md` — 판정 사전계산
- `mobile/scripts/README-coupang.md` — 쿠팡 갱신 절차와 한도
- `worker/README.md` — 워커 구조
