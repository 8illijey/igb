# 이거비싸? (IGB) — AI-Native Design Workflow 포트폴리오

---

## 01 · 표지 — AI와 함께 디자인하는 방식을 만들고, 컨슈머 앱 출시로 검증

- KAMIS 시세 기반, "예년 대비 싼지/평소인지/비싼지"를 판별해주는 컨슈머 앱
- 단일 코드베이스로 Web·iOS·Android 대응 (Expo / React Native / Expo Router)
- 역할: 기획 · 디자인 판단 · 디자인 시스템 · 프론트엔드 전반
- 핵심: AI를 발산·구현 도구로 두고, 제품·디자인 판단은 사람이 쥐는 작업 방식의 정립과 출시 검증

---

## 02 · 문제 정의 — 가격표는 보이는데, 비싼지 싼지 판단할 기준은 부재

- 절대 가격만으로는 "지금이 살 때인지" 판단 불가
- 필요한 건 "평소(예년) 대비 상대 위치"라는 기준점
- "그럼 지금 싼 재료로 뭘 만들지"라는 다음 행동까지 연결 필요
- 해결 범위: 예년 대비 시세 신호 + 싼 재료 기반 레시피 추천을 한 앱에 통합

---

## 03 · 리서치·발산 — Claude를 발산·반박 파트너로, 제품 판단은 디자이너가

- 신호 기준(임계값·기준선), 레시피 생성 방식, 데이터 표현안 등 후보를 빠르게 발산
- AI에게 반대 근거·대안 비교를 요청해 선택지 압축
- 채택·기각은 도메인·데이터·사용자 맥락으로 사람이 결정
- 예: "주1회 자동 생성" 후보를 이미지 갱신 제약을 근거로 기각, "고정 라이브러리 + 라이브 랭킹"으로 선회

---

## 04 · 디자인 결정·시그널 — 가격을 신호등으로 환원

- 예년 대비 ±10% 기준: -10% 이하 = 쌈(초록) / +10% 이상 = 비쌈(빨강) / 그 사이 = 평소(중립 회색)
- 어제 대비는 방향(±2%) 기준 — 하루 변동은 작아도 오름/내림이 유의미
- 색 토큰 3종 세트(`signal/X`, `signal/X-weak`, `text/on-signal-X`)로 칩·차트·배경 일관 적용
- 문구 위계: 그날 1위 = "할인율 1위" / 그 외 싼 품목 = "저렴해요" (최상급은 1위 전용)

---

## 05 · 워크플로우 — AI-Native 빌드 파이프라인

- 흐름: 결정 문서(토큰·신호 규칙·컴포넌트 규칙) → Figma MCP(컴포넌트·변수 생성/수정) → Claude Code(React Native 구현) → 실사용·디자인 기준 검증 → 규칙으로 환류
- Figma ↔ 코드 1:1 매핑: Figma 변수 = `theme/tokens.ts`, Figma 컴포넌트 = RN 컴포넌트
- 단방향 자동화가 아닌 양방향 동기화 (코드 변경 → Figma 반영, Figma 결정 → 코드 반영)
- 반복 가능한 자산 파이프라인: 사용자 생성 이미지 → 파일명 정규화(NFC) → KAMIS 코드 매핑 → 최적화 → 코드 주입

---

## 06 · design.md — 결정을 문서로, 문서를 AI 사양서로

- 단일 `design.md`(약 676줄)에 모든 결정을 "고정된 규칙"으로 기록 → AI·Figma·코드의 공통 입력 사양
- 구성: Brand & Style / Colors(Signal·Brand·Stage·Semantic) / Typography(타입 램프 + 판단 강조 패턴) / Spacing / Rounded / Border Width / Elevation & Depth(Glass pill recipe·LINEAR_BURN) / Motion / Shapes·Iconography / Components / Do's & Don'ts / Responsive / Known Gaps / References
- 컴포넌트별 사양을 문서에 직접 정의(signal-chip·price-list-row·comparison-toggle·detail-chart 등) → 구현의 사양서로 그대로 투입
- 색은 "신호등 = 유일한 강조 축" 원칙 + 결정 배경(예: fair 중립화 2026-06-13) 명시 → 이후 변경 시 같은 기준 재적용
- 동반 문서: `product.md`·`components.md`·`ux-writing.md` 보유 (역할 분담 상세: [확인 필요])

---

## 07 · AI 출력 검증 (히어로) — 코드는 통과하나, 디자이너만 잡은 것

> 빌드·타입은 통과하지만 도메인·데이터·언어·실사용 기준으로는 틀린 사례. 전부 사람 눈으로만 잡힌 지점.

- **"제일 저렴해요"의 최상급 오용** — 모든 싼 품목 상세에 동일 라벨 출력(코드 정상). 그날 할인율 1위에만 써야 하는 최상급이라 위계상 오류 → 1위 = "할인율 1위", 그 외 = "저렴해요"로 분리
- **어제 대비에 예년용 ±10% 밴드 적용** — 임계값 한 벌 재사용으로 빌드는 통과. +5% 일변동이 "평소 수준" 칩 + "5% 비싸졌어요" 요약으로 모순 → 어제 대비를 방향(±2%) 기준으로 분리
- **차트 y축의 작은 변동 과장** — 수치상 정확하나, 평소 수준(±소폭)이 바닥↔천장 급등처럼 보여 오해 유발 → 표시 폭 하한(기준가 비례) 도입
- **기준선 라벨 위치 불일치** — "예년" 라벨이 점선 실제 위치와 무관하게 상단 고정. 품목에 따라 선과 텍스트 어긋남 → 라벨을 점선 위에 직접 배치
- **차트 '오늘 값' ≠ 헤드라인 가격** — 차트(일별 시계열)와 헤드라인(카테고리 당일가)의 출처가 달라 값 불일치(각각은 정상) → 차트 끝점을 헤드라인 값으로 통일
- **"오늘이 가장 싸요" 오표기** — 어제가 더 쌌던 품목에서 결론 문구가 사실과 불일치 → 두 점 비교/추이 해석으로 로직 분리
- **단위 의미 오류** — 무게 없는 품목(망고 1개) 100g 환산 누락, 우유를 g으로 표기 → 평균중량 추정 + 우유는 mL
- **최근 검색어 저장 대상 오류** — '배추' 검색 후 '얼갈이배추' 선택 시 검색어('배추')가 저장 → 선택한 품목명 저장으로 변경
- **동작 없는 info 아이콘의 오인 어포던스** — 누를 수 있어 보이나 기능 부재 → 제거
- **컴포넌트 출처 오선택** — 외부 라이브러리 버튼 사용 → 자체 디자인 시스템 버튼(+`quiet` variant)으로 교체

---

## 08 · 근거·세션 로그 — 줄어든 비용과 줄지 않은 비용의 분리

- **구조(레이아웃) 구현 비용**: 토큰·컴포넌트 재사용으로 신규 화면일수록 감소 — 새 화면이 `price-list-row`·`signal-chip`·토큰 조합으로 빠르게 성립
- **판단·표현 반복 비용**: 감소하지 않고 데이터 시각화(차트)·AI 레시피 등 복잡 화면에 집중 (위치 무관)
- 반복 라운드(세션 대화 기준 근사): 레시피 ~15 / 품목 상세 ~12 / 홈 ~10 / 검색 ~8 / 디자인 시스템 ~8 / 웹·배포 ~6 / 관심목록 ~3
- 반복 캐치(07)가 별칭·임계값·규칙으로 흡수되며 동일 실수 재발은 감소 (정성적)
- 화면당 소요 시간 / 금전 비용: 측정 불가 (타임스탬프·과금 데이터 부재)
- 결론: "급감"은 구조 비용에 한정, 판단 반복 비용은 복잡도에 비례

---

## 09 · 결과물 — 5개 화면, 동일한 판단 규칙

- 홈: 그날 할인율 1위 카드 + 주목할 시세 그리드, 상단 글래스 고정 헤더
- 검색: 실시간 필터 / 검색 전 = 최근 검색어 + 예년보다 싼 TOP 10
- 관심목록: 관심 품목 + 관심 레시피(하트 저장), 시세 출처 표기
- 레시피(목록·상세): 라이브 시세로 필터·랭킹, 재료별 분량 비례 가격, 사진
- 품목 상세: 소매/도매/친환경, 예년·어제 대비 신호, 추이 차트, 단가·판매처별 가격
- 전 화면 공통: 동일 신호 규칙·토큰·문구 위계 적용
- 배포: Web → Vercel 완료 / Native → EAS 예정
- 화면 수 표기(5): 상세 화면 포함 기준 — [확인 필요]

---

## 10 · 회고 — AI 역할 경계, 그리고 다음

이 프로젝트의 목표는 멋진 앱 하나가 아니라, "AI와 함께 일하는 방식이 실제 출시까지 버티는가"를 확인하는 것이었다. 결론부터 말하면, AI는 발산과 구현에서 분명히 빨랐지만 제품을 옳게 만드는 판단은 끝까지 사람의 몫이었다.

AI가 잘한 영역은 명확했다. 후보를 빠르게 늘리고, 반대 근거를 만들고, 결정된 규칙을 코드와 Figma로 옮기는 일이다. 반대로 코드·타입은 통과하지만 디자인 기준으로는 틀린 것들 — 최상급 문구의 오용, 어제 대비에 잘못 적용된 임계값, 작은 변동을 과장하는 차트, 출처가 달라 어긋난 숫자, 어색한 한국어, 동작 없는데 눌러보고 싶은 아이콘 — 은 전부 사람이 잡았다. 공통점은 "맞아 보이는 것"과 "실제로 맞고 잘 읽히는 것" 사이의 간극이었고, 그 간극은 도메인·데이터·언어·실사용 감각으로만 메워졌다.

그래서 나는 AI의 역할을 "판단의 대행자"가 아니라 "판단을 빠르게 시험하게 해주는 도구"로 두는 것이 맞다고 생각한다. 결정을 문서와 토큰으로 고정해두면 AI의 출력 편차가 줄고, 반복되던 실수는 규칙으로 흡수되어 같은 검토를 두 번 하지 않게 된다. 다음에는 이 검토 자체를 더 체계적인 체크리스트로 남겨, 사람이 잡아야 할 지점에 시간을 더 쓰고 싶다.
